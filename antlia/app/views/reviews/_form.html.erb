<%= form_with(model: review) do |form| %>
  <% if review.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :rating, style: "display: block" %>
    <%= form.number_field :rating %>
  </div>

  <div>
    <%= form.label :likes, style: "display: block" %>
    <%= form.number_field :likes %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select(:user_id, options_for_select(User.pluck(:name, :id))) %>
  </div>

  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.select(:movie_id, options_for_select(Movie.pluck(:title, :id))) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
